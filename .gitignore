# Rendered carousels (output, not source)
carousel/
*.tmp

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only used to render the OG card)
node_modules/
npm-debug.log*

# Secrets — never commit
.env
.env.*
